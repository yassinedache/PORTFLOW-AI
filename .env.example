# ─── Database ─────────────────────────────────────────────────────────────────
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/portflow_ai?schema=public"

# ─── JWT ──────────────────────────────────────────────────────────────────────
JWT_ACCESS_SECRET="change-me-access-secret-key-min-32-chars!!"
JWT_REFRESH_SECRET="change-me-refresh-secret-key-min-32-chars!!"
JWT_ACCESS_EXPIRY="15m"
JWT_REFRESH_EXPIRY="7d"

# ─── QR Token ─────────────────────────────────────────────────────────────────
QR_TOKEN_SECRET="change-me-qr-token-secret-key!!"

# ─── CSRF ─────────────────────────────────────────────────────────────────────
CSRF_SECRET="change-me-csrf-secret"

# ─── App ──────────────────────────────────────────────────────────────────────
PORT=3000
NODE_ENV=development
CORS_ORIGIN=http://localhost:3001

# ─── AI (LLM) ────────────────────────────────────────────────────────────────
# Supported providers: openai, openrouter (set AI_API_KEY to enable LLM mode)
# Without AI_API_KEY the system uses a built-in rule-based engine
#
# For OpenAI direct:
#   AI_PROVIDER=openai
#   AI_BASE_URL=https://api.openai.com/v1
#   AI_API_KEY=sk-...
#   AI_MODEL=gpt-4
#
# For OpenRouter (free models available):
#   AI_PROVIDER=openrouter
#   AI_BASE_URL=https://openrouter.ai/api/v1
#   AI_API_KEY=sk-or-v1-...
#   AI_MODEL=openai/gpt-oss-120b:free
#
AI_PROVIDER=openrouter
AI_BASE_URL=https://openrouter.ai/api/v1
AI_API_KEY=your-api-key
AI_MODEL=openai/gpt-oss-120b:free

# ─── Blockchain ───────────────────────────────────────────────────────────────
BLOCKCHAIN_ENABLED=false
